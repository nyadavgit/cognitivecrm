# install the package if you don't have it
#install.packages("jsonlite")
library(jsonlite)
setwd("C:/dev/githubwork/cognitivecrm/R-scripts")
myjson <- toJSON(iris, pretty=TRUE)
cat(myjson)
inputData <- fromJSON('output10json.txt')
